// Package core contém a lógica de negócio do keyward: parsing e escrita do
// ~/.ssh/config, geração e reconciliação de chaves, metadata de expiração e
// backup/restore. Não depende de nenhuma das interfaces (CLI, TUI, GUI).
package core
