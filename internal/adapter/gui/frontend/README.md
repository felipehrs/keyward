# frontend

Placeholder — o projeto de frontend (HTML/CSS/JS) será gerado aqui via `wails init`/`wails3 init`
quando a GUI começar a ser implementada. Por enquanto este diretório existe só para refletir a
estrutura de pacotes definida na spec (seção 3.3).
